version: '3.8'

services:
  mysql:
    image: mysql:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: planta123
      MYSQL_DATABASE: planta
    ports:
      - 3333:3306
    
  api:
    build: .
    restart: always
    depends_on:
      - mysql
    ports:
      - 5045:5000